# Database (must match POSTGRES_* for Docker)
DATABASE_URL="postgresql://kyung:kyung_secret@localhost:5433/kyung_db?schema=public"

# Docker Postgres (docker-compose reads these)
POSTGRES_USER=kyung
POSTGRES_PASSWORD=kyung_secret
POSTGRES_DB=kyung_db
POSTGRES_PORT=5433
